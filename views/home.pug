extends layout

block content
  hr
  center
    p Please Scan Barcode of Next Badge
  hr

  h1 Attendee Checkin
  
  div
    div.overlay
    form(action="/", method="GET")
      input(type="text", name="id").alwaysfocus
      input(type="submit", name="checkin", value="Check In").btn.btn-success.btn-large